language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t cytechservices/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true cytechservices/docker-react npm run test